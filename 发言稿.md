# 可转债转股价下修与提前强赎双事件全生命周期跟踪及结构化分析

## 发言稿

尊敬的老师、各位同学，大家好。

我是 XX。今天我汇报的项目是《可转债转股价下修与提前强赎双事件全生命周期跟踪及结构化分析》。这个项目的目标，是从巨潮资讯网公开公告中，自动归集可转债相关公告，解析 PDF 文本，抽取关键字段，并把分散在不同公告里的下修和强赎事件尽量串成可分析的事件链。

我选择这个题目，是因为可转债兼具债券和股票期权属性，而转股价下修和提前强赎，是可转债存续期中最重要的两类事件。下修会改变转股价值，强赎会影响投资者是否及时转股。这些信息都披露在公告里，但公告数量多、格式不统一，而且大多是 PDF 文档。人工逐篇阅读可以做，但很慢，也不容易形成统一的数据表。因此，我希望用数据挖掘和大语言模型，把这些非结构化公告转成可追溯、可比较、可复核的结构化数据。

本项目目前使用的正式数据口径是：只认定来自巨潮资讯网公开公告的数据为正式数据，示例数据、合成数据、人工待复核数据都不能直接进入正式 metadata。按照当前仓库输出，正式 metadata 一共有 1171 条，本地 PDF 文件 456 份，MinerU 解析得到 Markdown 442 份。经过结构化抽取和 Pydantic 校验后，得到 384 条有效抽取记录，其中下修类公告 203 条，强赎类公告 181 条。这 384 条记录中，转债代码和转债名称的填充率都是 100%，证据文本 `evidence_text` 覆盖 383 条，覆盖率为 99.7%。

接下来我介绍一下整体技术流程。

第一步是公告抓取。我使用巨潮资讯网公开公告作为主数据来源，通过关键词检索与 metadata 标准化，保存公告标题、发布日期、公告链接、PDF 链接等信息。爬取过程设置了限速，不绕过登录、验证码或访问限制。

第二步是 PDF 解析。项目使用 MinerU 将公告 PDF 转成 Markdown，保留文本结构，为后续章节定位和字段抽取做准备。当前本地有 456 份 PDF，已解析 Markdown 442 份。

第三步是章节定位。公告里有大量格式化但并不都相关的内容，比如风险提示、释义、公司基本信息等。项目通过规则定位转股价格、赎回条款、重要日期等关键区域，减少 LLM 需要处理的噪声。

第四步是 LLM 字段抽取。项目设计了 25 个核心字段，包括公共字段、下修专属字段和强赎专属字段。例如 `bond_code`、`bond_name`、`ann_type`、`original_conv_price`、`new_conv_price`、`redemption_price`、`record_date` 等。这里我特别强调证据机制：关键字段不仅要给出值，还要尽量保留 `evidence_text`，也就是公告原文依据。

第五步是 Pydantic 校验。抽取后的数据会经过类型、日期格式、数值范围和业务逻辑校验。比如日期统一为 `YYYY-MM-DD`，价格单位统一为元/股或元/张，下修幅度和赎回溢价率要按照公式计算。无法判断的字段保留为空，而不是强行补值。

第六步是事件链匹配和指标计算。项目根据转债代码、事件类型和节点信息，把同一转债的公告聚合成事件链。当前主流程重新生成后，共得到 169 条事件链，其中下修事件链 121 条，强赎事件链 48 条。系统识别到 28 条四节点覆盖链，占 16.6%。这里我没有把它直接称为“完全准确的完整生命周期”，而是称为“四节点覆盖链”，因为它还需要人工确认这些节点是否一定属于同一事件周期。

量化指标方面，项目会计算下修幅度和赎回溢价率。当前指标文件共有 169 条记录。下修幅度可计算记录为 25 条，赎回溢价率可计算记录为 48 条。由于部分公告缺少转股价值或正股均价，赎回溢价率中存在简化计算口径，这一点在报告和展示页中也做了说明。

评估方面，我现在补充完成了 50 条公告级人工抽样复核。复核方式不是只看模型输出，而是把样本逐条对照正式抽取结果、本地 PDF 文本和 MinerU 解析 Markdown，并生成了字段级复核表 `outputs/eval/manual_sample_review.csv`。这次一共形成 350 条字段级复核记录。结果显示：转债或债券代码 50 条全对，股票代码 48 条完全正确、1 条部分正确、1 条错误；日期字段内容准确性是 44 条完全正确、5 条部分正确、1 条错误；价格数值严格准确性是 33 条完全正确、16 条部分正确、1 条错误；证据文本相关性是 47 条完全正确、2 条部分正确、1 条错误。

在阶段分类上，我也做了一轮稳妥优化。原来 metadata 中有 700 条 `stage_unknown`，现在通过公告类型和标题关键词规则，全部补充到触发、提议、决议、实施、结果等标准阶段。补完后 `stage_unknown` 为 0。不过这不代表每条都已经人工确认无误，所以我同时输出了阶段复核队列，里面包含范围检查、本地 PDF 缺失和重复 `doc_id` 等需要后续人工确认的记录。

在项目过程中，我遇到的第一个重要问题是数据真实性问题。早期项目中曾经混入过示例或生成痕迹，例如一些看起来像真实转债但无法追溯到公告的数据。这个问题让我意识到，AI 生成内容不能直接当数据使用。因此我后来增加了真实数据守卫：正式 metadata 必须来自 `cninfo.com.cn` 或 `static.cninfo.com.cn`，不能包含 sample、synthetic、generated、随机、示例、未知公司、未知转债等标记。当前正式 `data/metadata/metadata.csv` 校验结果为 1171 条，真实来源违规为 0 条。

第二个问题是“看起来很完整”的指标不一定可信。比如事件链如果只按转债代码聚合，可能会把同一转债不同时间的公告放到一条链里。因此我在最新版本中把“完整链”改成了更谨慎的“四节点覆盖链”，并在可视化页面里标注“需人工复核是否属于同一事件周期”。这比直接写一个很高的准确率更诚实，也更符合数据挖掘项目的复核要求。

第三个问题是人工复核暴露出字段质量并不均衡。公共字段整体比较好，比如 `bond_code`、`bond_name`、`ann_type` 都是 100%；但是 `evidence_page` 只有 243/384，覆盖率 63.3%。这说明项目虽然已经能提取大部分证据文本，但页码级追溯还有提升空间。人工样本还发现了几类更具体的问题：50 条样本中有 7 条需要做普通公司债或范围复核；有些赎回价格的小数位被压缩，比如 101.3973 被写成 101.4；还有个别公告把历史下修价格混入当前提议阶段，比如龙大转债的提议公告里不应该把历史生效日当成本次生效日。这些问题说明，后续不仅要提高字段覆盖率，还要做更细的事件阶段识别和证据校验。

项目的主要亮点，我总结为三点。

第一，真实数据口径比较干净。项目现在明确区分正式数据、人工复核数据和示例数据，正式结果只认巨潮资讯网公开公告。

第二，流程是可复现的。现在可以通过 `pipeline_run.py` 分步运行标准化、校验、事件链匹配和指标计算。比如本次展示页使用的数据，就是重新运行 `process` 和 `indicator` 后得到的 169 条事件链和 169 条指标记录。

第三，结果是可追溯、可复核的。项目不是只给一个字段值，而是尽量保留公告证据文本，并通过 Pydantic 校验、错误输出和人工抽样表来检查字段是否真的来自公告原文。现在已经有 `eval_manual_sample.csv`、`manual_sample_review.csv`、`manual_sample_review_summary.csv` 和 `manual_error_cases.md` 这些评估文件，可以直接看到每条样本哪里对、哪里只是部分正确、哪里需要修。

当然，这个项目还有明显不足。

第一，人工复核样本量虽然已经从 20 条扩展到 50 条，但仍不能直接代表全部 384 条抽取记录的全量准确率。所以我没有把它包装成“整体准确率很高”的结论，而是按维度报告：代码字段和证据文本表现较好，价格精度、公告细分阶段和样本范围仍需优化。

第二，事件链还需要更细的事件周期识别。当前 28 条四节点覆盖链是系统规则识别结果，不代表已经人工确认。后续需要加入更严格的时间窗口、公告标题语义、董事会和股东大会日期等约束。

第三，证据页码和部分金融字段仍需提升。尤其是 `evidence_page`、`new_conv_price`、`effective_date`、`last_convert_date` 等字段，后续要继续优化章节定位和提示词。

最后，我想说一下这个项目带给我的收获。技术上，我学会了如何把爬虫、PDF 解析、LLM 抽取、Pydantic 校验、事件链匹配和可视化串成一个完整管线。方法上，我最大的体会是：数据挖掘项目不能只追求漂亮的指标，必须保证每个数字都有来源，每个字段都能解释，每个结论都能复核。特别是在金融场景里，宁愿承认字段缺失，也不能用看似合理的数据去填补真实证据。

以上就是我的项目汇报。谢谢老师和同学们的聆听，欢迎大家批评指正。

---

## 展示时可强调的真实数据口径

- 正式 metadata：1171 条，来源为巨潮资讯网公开公告。
- 本地 PDF：456 份。
- Markdown 解析结果：442 份。
- 已校验抽取记录：384 条，其中下修类 203 条，强赎类 181 条。
- 事件链：169 条，其中下修 121 条，强赎 48 条。
- 四节点覆盖链：28 条，覆盖率 16.6%，但仍需人工确认是否同一事件周期。
- 指标记录：169 条。
- `evidence_text`：383/384，覆盖率 99.7%。
- `evidence_page`：243/384，覆盖率 63.3%，这是当前短板。
- 阶段分类扩展：`stage_unknown` 从 700 条降为 0 条，同时输出 948 条阶段/范围复核队列。
- 人工抽样复核：50 条公告级样本，350 条字段级复核记录。
- 抽样结果：转债/债券代码 50/50，股票代码 48/50 完全正确，日期字段内容 44/50 完全正确，证据文本相关性 47/50 完全正确。
- 抽样发现：7 条需要样本范围复核，价格字段 33/50 完全正确、16/50 部分正确、1/50 错误。

## 可能被问到的问题

**1. 为什么不直接说有 28 条完整事件链？**  
因为当前系统的 `complete=True` 表示四类节点都被覆盖，但事件链是按转债代码和事件类型聚合的，还需要人工确认这些节点是否属于同一轮事件。所以答辩时建议说“四节点覆盖链”，不要说“人工确认完整链”。

**2. 现在做了人工复核，为什么不直接说一个总体人工准确率？**  
因为这次是 50 条公告级抽样，不是 384 条全量人工标注。为了严谨，我把结果拆成不同维度：转债或债券代码是 50/50，股票代码是 48 条完全正确、1 条部分正确、1 条错误；但价格数值严格准确性是 33 条完全正确、16 条部分正确、1 条错误，说明金融字段还需要继续修正。因此我更愿意报告可追溯的抽样结果，而不是给一个容易误导的总准确率。

**3. 项目最大的不足是什么？**  
事件链周期识别、普通公司债过滤、价格精度保留和证据页码追溯还需要加强。当前项目已经完成主流程和一轮人工抽样复核，但后续还需要扩大人工样本，并把人工发现的问题反向用于规则和提示词优化。

**项目代码仓库**：https://github.com/Icebergreveal/convertible-bond-tracker

**建议演讲时间**：约 10-12 分钟。
